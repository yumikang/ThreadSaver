# 🚨 긴급 수정: 콘텐츠 스크립트가 로드되지 않는 문제

## 문제 증상
- 서비스 워커는 정상 작동
- 트위터 페이지 콘솔에 🧵 로그가 전혀 없음
- "Could not establish connection" 에러 발생

## 원인
Chrome이 x.com에 대한 콘텐츠 스크립트를 주입하지 않음

## 해결 방법

### 방법 1: 익스텐션 권한 확인 (가장 가능성 높음)

1. `chrome://extensions/` 열기
2. ThreadSaver 찾기
3. **"세부정보"** 버튼 클릭
4. 아래로 스크롤해서 **"사이트 액세스"** 섹션 찾기
5. 다음 중 하나를 선택:
   - **"모든 사이트에서"** (권장)
   - 또는 **"특정 사이트에서"** → `x.com`과 `twitter.com` 추가

6. 트위터 페이지 새로고침 (Cmd+Shift+R)
7. F12 콘솔에서 🧵 로그 확인

---

### 방법 2: 익스텐션 완전 재설치

1. `chrome://extensions/`에서 ThreadSaver **제거**
2. Chrome 완전히 종료
3. Chrome 재시작
4. `chrome://extensions/` 열기
5. **"개발자 모드"** 토글 켜기 (우측 상단)
6. **"압축해제된 확장 프로그램을 로드합니다"** 클릭
7. `/Users/blee/Desktop/blee-project/threadsaver/extension` 폴더 선택
8. 권한 요청 나오면 **"허용"** 클릭
9. 트위터 페이지로 이동
10. F12 콘솔에서 🧵 로그 확인

---

### 방법 3: manifest.json에 명시적 권한 추가

현재 manifest.json은:
```json
"permissions": [
  "activeTab",
  "storage"
]
```

이것을 다음으로 변경:
```json
"permissions": [
  "activeTab",
  "storage",
  "scripting"
]
```

변경 후:
1. 파일 저장
2. `chrome://extensions/`에서 익스텐션 새로고침
3. 트위터 페이지 새로고침

---

## 확인 방법

### ✅ 성공적으로 작동하는 경우
트위터 페이지 F12 콘솔에서:
```
🧵 ThreadSaver: Content script INITIALIZING...
🧵 ThreadSaver: Current URL: https://x.com/_beomlam/status/1719717507702124632
🧵 ThreadSaver: Hostname: x.com
🧵 ThreadSaver: Content script loaded and listeners registered
🧵 ThreadSaver: Page fully loaded, content script ready
🧵 ThreadSaver: ✅ Content script fully ready and waiting for messages!
```

### ❌ 여전히 실패하는 경우
아무 로그도 안 보임

---

## 추가 디버깅

만약 위 방법이 모두 실패하면:

1. **Sources 탭에서 확인**:
   - F12 → Sources 탭
   - 왼쪽 트리에서 "Content scripts" 찾기
   - "ThreadSaver" 또는 "twitter-scraper.js" 있는지 확인

2. **수동으로 스크립트 주입 테스트**:
   트위터 페이지 콘솔에서 직접 입력:
   ```javascript
   console.log('🧵 ThreadSaver: Manual test');
   ```

   이게 나타나면 콘솔은 정상 작동하는 것

3. **익스텐션 ID 확인**:
   - manifest.json에 `"key"` 필드가 없어야 함
   - 있으면 제거하고 재설치
